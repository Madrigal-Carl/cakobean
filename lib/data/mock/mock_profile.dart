import 'package:cakobean/domain/models/profile.dart';

/// Temporary mock data until this is wired to a real data source.
const mockProfile = ProfileModel(
  firstName: 'Demo',
  lastName: 'Farmer',
  username: 'demo',
);
